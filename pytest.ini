[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: requires a real language-model API key and network access
filterwarnings =
    ignore::DeprecationWarning
